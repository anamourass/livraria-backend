# Production Configuration for Azure
server.port=${PORT:8080}

# CORS Configuration
# cors.allowed-origins=${CORS_ALLOWED_ORIGINS:https://yourapp.azurestaticapps.net}

# PostgreSQL Database configuration
spring.datasource.url=${DATABASE_URL:jdbc:postgresql://dpg-d3ndi4q4d50c73cjih40-a.oregon-postgres.render.com/livraria_gyhu}
spring.datasource.driverClassName=org.postgresql.Driver
spring.datasource.username=${DB_USERNAME:livraria_gyhu_user}
spring.datasource.password=${DB_PASSWORD:VZPleDgSMJlOqPrEm7LRzA2L1mQpoNHp}

# JPA/Hibernate configuration for PostgreSQL
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.defer-datasource-initialization=true

# SQL initialization
spring.sql.init.mode=always

# Logging configuration
logging.level.com.livraria=INFO
logging.level.org.springframework.web=INFO
logging.level.root=WARN

# Health check endpoint
management.endpoints.web.exposure.include=health,info
management.endpoint.health.show-details=when-authorized

# Application info
info.app.name=Livraria Backend API
info.app.description=API REST para gerenciamento de livraria
info.app.version=1.0.0